name: pipeline

on:
  push:
    branches:
      - main
      - dev

jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install python
        run: docker-compose -f docker-compose.yml up -d --build